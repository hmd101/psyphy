import sys
from pathlib import Path

"""
for fixtures and pytest hooks 
"""

# # Add src to sys.path so tests can import without editable install
# ROOT = Path(__file__).resolve().parents[1]
# sys.path.insert(0, str(ROOT / "src"))
